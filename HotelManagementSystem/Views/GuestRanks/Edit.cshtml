@model EditRankFormModel

@{
    ViewData["Title"] = "Edit rank";
}

<div class="row">
    <h4 class="heading-margin text-center">Edit rank</h4>
    <form method="post" class="col-md-4 offset-md-4 col-sm-12">
        <div class="form-group">
            <label asp-for="Name"></label>
            <input asp-for="Name" class="form-control" value="@Model.Name" />
            <span asp-validation-for="Name" class="text-danger small"></span>
        </div>
        <div class="form-group">
            <label asp-for="Discount"></label>
            <input asp-for="Discount" class="form-control" value="@Model.Discount" />
            <span asp-validation-for="Discount" class="text-danger small"></span>
        </div>
        <input type="hidden" value="@Model.Id" />
        <input type="submit" value="Save" class="btn btn-primary col-md-4 offset-md-4 col-sm-4 offset-sm-4" />
    </form>
</div>

@section scripts
{
    <partial name="_ValidationScriptsPartial" />
}